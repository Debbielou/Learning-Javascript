
export function add (a,b) {
  return a + b;
    
}

export function multiply (x, y) {
  return x * y;
}

export function divide (p, s) {
  return p / s;
}

//export one by one
export const name = "Jesse";
export const age = 40;

//export at once
const muntu = "Ethan";
const salary = 500;

export {muntu, salary};